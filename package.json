{
  "name": "zion-codex-pool",
  "version": "0.1.1",
  "description": "Local Codex account pooler — route Codex CLI/IDE through multiple ChatGPT subscriptions without swapping auth.json",
  "type": "module",
  "bin": {
    "zion-codex-pool": "./dist/cli/main.js"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "cli": "tsx src/cli/main.ts",
    "extension:install": "npm install --prefix extension",
    "extension:build": "npm run build && npm run build --prefix extension",
    "extension:package": "npm run build && npm run package --prefix extension"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/node": "^22.15.30",
    "@types/ws": "^8.18.1",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
